{
  "name": "my-static-app",
  "version": "1.0.0",
  "description": "A simple static site",
  "scripts": {
    "start": "live-server",
    "build": "echo 'No build step needed for static site'"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
